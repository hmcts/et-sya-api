#!groovy

@Library("Infrastructure")

def type = "java"
def product = "et"
def component = "sya-api"

withPipeline(type, product, component) {
  disableLegacyDeployment()
  enablePactAs([PactRoles.CONSUMER])
}
